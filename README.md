# pareto-vimeo
Using prompt evaluation to create a pareto frontier for the most efficient AI configurations for video transcript generation. So far, results show that the lowest WER score is achieved by using a custom prompt for the whisper-v1 model. But, a far cheaper model, whisper-large-v3-turbo is far cheaper to run and can achieve a WER that is not too much worse.
